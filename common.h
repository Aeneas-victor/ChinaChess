
#pragma once
#include<iostream>
#include<Windows.h>
#include<graphics.h>
#include<thread>
#include<map>
#include<math.h>
#include<algorithm>
#include <mmsystem.h>
#pragma comment(lib, "winmm.lib")
#define SQU(x) ((x)*(x))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a,b) ((a)>(b)?(a):(b))
#define CHESS_SIZE 70
#define INTERVAL 50
#define ROW 10
#define COL 9
